<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Settings</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background: url('/images/b322.jpg') no-repeat center center fixed;
            background-size: cover;
        }

        .settings-container {
            max-width: 800px;
            margin: 50px auto;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }

        .settings-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .settings-header h2 {
            color: #333;
        }

        .settings-actions .btn {
            margin: 10px;
            width: 200px;
        }
    </style>
</head>

<body>

    <div class="container settings-container">
        <!-- Settings Header -->
        <div class="settings-header">
            <h2>Account Settings</h2>
        </div>

        <!-- Settings Actions -->
        <div class="settings-actions text-center">
            <a href="/edit/user/<%= user._id %>" class="btn btn-primary">Edit Profile</a>
            <a href="/change-password/user/<%= user._id %>" class="btn btn-warning">Change Password</a> <!-- Change Password Button -->
            <a href="/logout" class="btn btn-danger">Logout</a>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
