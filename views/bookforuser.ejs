<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User's Book Collection</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background: url('/images/b32.jpeg') no-repeat center center fixed;
            background-size: cover;
            color: white;
        }

        .table-container {
            max-width: 1200px;
            margin: 50px auto;
            background-color: rgba(255, 255, 255, 0.8); /* Transparent white background */
            padding: 20px;
            border-radius: 10px;
        }

        .table thead th {
            background-color: #007bff;
            color: white;
        }

        .table tbody td {
            vertical-align: middle;
            color: black; /* Ensure text in table is visible */
        }

        .book-image {
            height: 100px;
            width: auto;
            object-fit: cover;
        }
    </style>
</head>

<body>

    <div class="container table-container">
        <h1 class="text-center mb-4">Book Collection</h1>
        <table class="table datatable">
            <thead>
                <tr>
                    <th><b>Book</b> Name</th>
                    <th>Author Name</th>
                    <th>Publisher Name</th>
                    <th>Language</th>
                    <th>Edition</th>
                    <th>ISBN</th>
                    <th>Origin</th>
                    <th>No. of Pages</th>
                    <th>Price Per Unit</th>
                </tr>
            </thead>
            <tbody>
                <% books.forEach((book)=> { %>
                <tr>
                    <td><%= book.bookTitle %></td>
                    <td><%= book.bookAuthor %></td>
                    <td><%= book.publisherName %></td>
                    <td><%= book.language %></td>
                    <td><%= book.edition %></td>
                    <td><%= book.isbn %></td>
                    <td><%= book.origin %></td>
                    <td><%= book.noOfPages %></td>
                    <td><%= book.pricePerUnit %></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
