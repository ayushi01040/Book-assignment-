<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Setting</title>

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css?family=Nunito:400,600,700" rel="stylesheet">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background: url('/images/user.jpg') no-repeat center center fixed; 
            background-size: cover;
        }

        .panel-container {
            max-width: 900px;
            margin: 50px auto;
            background-color: #00FFFF;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }

        .panel-header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 2px solid #e0e0e0;
            margin-bottom: 20px;
        }

        .panel-header h2 {
            color: #333;
        }

        .user-info {
            margin-bottom: 30px;
        }

        .user-info p {
            margin: 5px 0;
            color: #555;
        }

        .user-actions {
            margin-top: 20px;
        }

        .user-actions .btn {
            margin: 10px;
        }

        .btn-logout {
            background-color: #dc3545;
            color: white;
        }
    </style>
</head>
<body>

    <div class="panel-container">
        <!-- Panel Header -->
        <div class="panel-header">
            <h1>Welcome, <%= user.firstName %> <%= user.lastName %>!</h1>
        </div>

        <!-- User Information -->
        <div class="user-info">
            <h4>Profile Information</h4>
            <p><strong>Email:</strong> <%= user.email %></p>
            <p><strong>Phone:</strong> <%= user.mobileNo %></p>
        </div>

        <!-- User Actions -->
        <div class="user-actions text-center">
            <h4>Actions</h4>
            <a href="/viewbook" class="btn btn-secondary w-auto">View Books</a>
            <a href="/user/settings/<%= user._id %>" class="btn btn-warning w-auto">Account Settings</a>
            <a href="/profile" class="btn btn-secondary w-auto">About Admin</a>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
